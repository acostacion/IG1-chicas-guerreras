P2:	
- La captura de la foto debería ser de toda la pantalla, no solo de donde se pinta en la escena.
- No está bien el viewport de la foto cuando amplias la ventana.

P3: (HEMOS MANDADO CORREO)
- El zoom con la rueda hace un movimiento lateral.
- No hay movimiento del ratón cuando hay dos vistas. 
- Como cambias de escena cuando teneis dos vistas? (nota: se refiere a en modo 2 escenas?)
- Tenéis mal pitch/roll/yaw. Hay que cambiar el look y el up. En yaw y pitch, ambos se rotan. En el roll solo se rota el up. Pero nunca se rota la PM

P4:
- Se ven las "costuras" donde se unen los calculos de las normales en las figuras de revolución. //Lo veremos más tarde
- Las alas de la nave espacial no son transparentes (Este error viene dado por añadirlo al compound entity del tie?).
- Hay fugas de memoria. (Ya se lo explicamos. Vienen de crear el nodo ficticio para la nariz del tie)

???
- No deberíais mover la nave desde la clase Scene. (Nota: Usando la técnica del nodo ficticio programa dos métodos en la clase Scene ?????)

P5:
- Múltiples errores por consola. Se muestran cuando intentáis cargar el shader del color al renderizar ColorMaterialEntity.
- Nunca llaméis un método de un método. Por ejemplo: cilinder->getMaterial().setDarkBlue(); //Lo veremos más tarde
- Quitad los shaders del CompoundEntity::CompoundEntity. No es necesario.
- Quitad la activación de las luces de Scene::load. Debería haber un método independiente que las activase (como pone en el enunciado de la práctica). Sin embargo, si podéis desactivarlas en Scene::unload.
- Todas las escenas comparten una luz direccional, que está activa por defecto y solo se modifica en la escena del planeta.
- AdvancedTiE deberá incluir en su clase la luz focal.
- Quitad glPolygonMode(GL_FRONT_AND_BACK, GL_FILL); de EntityWithTexture::render. No es necesario y si lo necesitáis es porque hace falta en otro sitio.
- ColorMaterialEntity no debería tener render propio, sino reutilizar el de EntityWithMaterial (que se queda con el dibujado de normales). 
- EntityWithMayerial no hace falta que tenga un atributo Shader
- No se puede cargar ninguna luz. Y cuando lo intentas, al final aparece un runtime error.








